#SAVEAS "@:DEFPX.SRC.PRG"
#REM 0
#INCLUDE "GPB.INC.BL"

## A call above its own declaration. The call carries an address, so this cannot work.

GP.SUB LATER, 1
END

GP.DEFPROC LATER, DP.N
DP.LATER:
PRINT DP.N
RETURN
