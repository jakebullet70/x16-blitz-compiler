#SAVEAS "@:DEFFNY.SRC.PRG"
#REM 0
#INCLUDE "GPB.INC.BL"

## GP.FN above its own GP.DEFPROC. The call carries an address, not a line number.

PRINT GP.FN(LATE, 1)
END

GP.DEFPROC LATE, L.A RETURNS L.R : GOSUB LATE.BODY
RETURN
LATE.BODY:
L.R = L.A
RETURN
