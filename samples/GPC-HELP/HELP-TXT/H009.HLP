@3. Command reference
.
=3. COMMAND REFERENCE
.
.30 tokens, $CE7F down to $CE58, allocated downward. Ten of the forty slots
.are holes, and they stay holes: the token values are the ABI and are never
.renumbered.
.
=At a glance -- where each part comes from
.
.Three implementations, and what each costs:
.
.  - ASM -- a keyword run by machine code in the runtime. Costs runtime
.    bytes, and one GP keyword anywhere in a program pays for the whole
.    1,024-byte block. Documented in SEC 3.
.  - BASIC -- a .INC.BL module of ordinary BASL, called with GOSUB. Costs
.    nothing unless #INCLUDEd, and then only its own p-code. Documented in
.    SEC 4.
.  - COMPOSITE -- a keyword with no machine code of its own; the compiler
.    expands it into keywords that already exist. Costs the runtime nothing.
.    Documented in SEC 3 with the ASM keywords.
.
.  Loops
.      ASM
.      GP.DO GP.LOOP GP.EXITDO
.
.  Multi-way branch
.      ASM
.      GP.SELECT GP.CASE GP.OTHER GP.ENDSEL
.
.  Block IF
.      ASM
.      GP.IF GP.ELSEIF GP.ELSE GP.ENDIF -- see SEC 3.8
.
.  Machine code
.      ASM
.      GP.CALL GP.A GP.X GP.Y GP.C
.
.  Inline assembly
.      COMPOSITE
.      GP.ASM GP.ENDASM -- free, and stays GP-BASIC OUT, see SEC 3.9
.
.  Strings
.      ASM
.      GP.INSTR GP.STRPTR GP.COMP
.
.  Strings
.      COMPOSITE
.      GP.CONTAINS GP.ISEMPTY -- free, see SEC 3.4
.
.  Addresses
.      COMPOSITE
.      GP.HIBYTE GP.LOBYTE -- free, see SEC 3.3
.
.  Arrays
.      ASM
.      GP.ARRPTR
.
.  Screen
.      ASM
.      GP.BOX GP.FILL GP.PRINTAT
.
.  Screen
.      COMPOSITE
.      GP.CHAR -- free, one cell in GP.PRINTAT's shape running GP.FILL's
.        handler
.
.  Colour roles
.      BASIC
.      THEME.INC.BL -- THEME.LOAD, THEME.CLR() - SEC 4.1
.
.  String helpers
.      BASIC
.      STRINGS.INC.BL -- PADR PADL PADC SPLIT REPLACE PET2SCR - SEC 4.2
.
.  Screen etiquette, panels
.      BASIC
.      APPSYS.INC.BL -- STARTUP RESTORE PANEL.SAVE/LOAD/PUT - SEC 4.3
.
.  Entry fields
.      BASIC
.      LINEINPUT.INC.BL -- LINEINPUT.GET, LINEINPUT.ASK - SEC 4.4
.
.  Bitmaps
.      BASIC
.      BMX.INC.BL -- BMX.SHOW, BMX.RESTORE - SEC 4.5
.
.  Menus
.      BASIC
.      MENUVERT.INC.BL -- RUN DRAW ROW HOTFIND - SEC 4.6
.
.The rule is in SEC 1: assembly for tight loops and bulk data moves, BASIC
.for everything else, and a composite for anything that is only a spelling of
.keywords already present. A menu waits on a human, so MENUVERT is BASIC; as
.a keyword it would cost every GP program 462 bytes whether or not it used a
.menu.
.
.The keywords in detail. Square brackets mean optional. Optionals cannot be
.skipped over: GP.BOX X,Y,W,H,,7 is a syntax error -- write out the default
.you are passing through.
.
=SEE ALSO
.
>35|4. Module reference -- the BASL library
>27|3.8 Block IF
>28|3.9 Inline assembly
>21|3.4 Strings
>19|3.3 Machine code
>36|4.1 THEME.INC.BL -- named colour roles
>37|4.2 STRINGS.INC.BL -- string helpers
>38|4.3 APPSYS.INC.BL -- start politely, leave it as you found it
>39|4.4 LINEINPUT.INC.BL -- a positioned entry field
>40|4.5 BMX.INC.BL -- a BMX bitmap into VERA
>41|4.6 MENUVERT.INC.BL -- a vertical menu
>2|1. What GP.BASIC is
