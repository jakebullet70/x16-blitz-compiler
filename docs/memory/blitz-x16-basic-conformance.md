---
name: blitz-x16-basic-conformance
description: Blitz-vs-stock-X16-BASIC conformance — 3 of the 4 defects are fixed; the float-literal one remains, root cause now known
metadata:
  type: project
---

Ran the [[gpc-x16-basic-coverage]] lexer-blocker list and the [[gpc-for-step0-semantics]] / [[gpc-if-semantics]] semantics
checks against the **compiled** Blitz output, with stock X16 BASIC (interpreted, R49) as the control. Method: tokenise with
`bin/tokenise.zip`, compile via `BLITZ.PRG` under `bin/x16emu`, run `OBJECT.PRG`, compare printed output. NB the emulator does
not self-exit for these, so each run must be `timeout`-capped; and `-bas` injection types through the keyboard, so control
programs must be **UPPERCASE**. A compiled program can end with `I2CPOKE 66,1,0` to power the machine off (Blitz has no
`POWEROFF` handler), which makes the emulator exit immediately.

**PASSES** (Blitz already handled these): hex `$FF` · binary `%1010` · decimals >= 65536 · identifiers > 7 chars ·
`IF` line semantics (a false `IF` correctly skips the WHOLE line).

## FIXED (2026-07-13)

- **`FOR ... STEP 0`** — was worse than recorded. `next.asm` branched on the step's sign BIT, giving only two cases, so a zero
  step took the ascending path and exited at once. AND `for.asm`'s optimised integer path did not exclude a zero step: its
  carry-propagating increment (`beq _CNOIncrement`) never terminates when adding 0 to a 0 byte, so `FOR I=0 TO 5 STEP 0`
  compiled fine and then **hung forever**, walking Y off the end of the 4-byte variable. Now a three-way test (exit iff
  `sign(compare) == sign(step)`, `sign(0)==0` -> exact equality only), and a zero step is kept out of the optimised path.
- **Reversed relationals** — `expression.asm` only inspected operators STARTING with `<` or `>`, and after `>` only looked for
  `=`. So `><`, `=<`, `=>` were syntax errors. All six spellings now fold to the three real operators.
- **Scientific notation** — `9.2E5` now compiles. See the commit; the trap is that **the exponent's sign is a TOKEN, not
  ASCII** (`$AB`, not `$2D`), because the encoder is fed tokenised text — testing for ASCII `"-"` made `1.5E-2` evaluate to
  `-0.5`, since the number stopped at `1.5` and the parser read the `$AB` as subtraction.

## STILL OPEN: float literals are one ULP low — **root cause found**

`A=0.5` prints `0.4999999` (stock BASIC: `.5`). Computed `1/2` IS exact, so it is the literal conversion, not the arithmetic.

**Why:** the fraction is built as `digits x 10^-dc` using `FloatScalarTable` (`source/ifloat32/source/generated/constants.asm`,
generated by `scripts/constants.py`). That table's entries are a shade **LOW** — `10^-1` is `$66666666 x 2^-34` =
`0.099999999976...` — so `5 x 10^-1` = `0.4999999998`, even though **0.5 is exactly representable**. The algorithm never forms
0.5 at all.

**The fix is probably to DIVIDE rather than multiply**: `digits / 10^dc` (5/10) can land exactly on 0.5, where
`digits x 10^-dc` cannot. Worth checking `FloatDivide` rounds correctly before committing to it. The same defect makes
`1.5E-2` print `0.0149999`; positive exponents avoid the table entirely and are exact (`9.2E5` = `920000` exactly).

**Related, and check it first:** `IF A=B` reports SAME even though `A-B <> 0` — iFloat32's compare and subtract disagree about
equality. Understand that before "fixing" either.

**Cosmetic:** Blitz PRINTs `0.5` / `3.1415900`; real X16 BASIC prints `.5` (no leading zero, no trailing zeros).

See [[blitz-x16-r44-plus-keywords]] and [[blitz-x16-build-setup]].
